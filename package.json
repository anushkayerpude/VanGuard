{
  "name": "vanguard",
  "version": "1.1.0",
  "private": true,
  "description": "VANGUARD — Multi-Source Defence Situational Awareness System (HackHertz 2026 D-05)",
  "type": "module",
  "scripts": {
    "dev": "concurrently -n \"SERVER,CLIENT\" -c \"cyan.bold,magenta.bold\" \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "smoke": "npm run smoke --prefix server",
    "test": "npm test --prefix server",
    "build": "npm run build --prefix server && npm run build --prefix client",
    "typecheck": "npm run typecheck --prefix server && npm run build --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
